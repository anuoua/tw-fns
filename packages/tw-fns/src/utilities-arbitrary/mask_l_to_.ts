/**
 * - mask-image: linear-gradient(to left, black var(--tw-mask-left-from), transparent ${arbitrary});
 */
export const mask_l_to_ = (arbitrary: string) => () => `  mask-image: linear-gradient(to left, black var(--tw-mask-left-from), transparent ${arbitrary});`;
