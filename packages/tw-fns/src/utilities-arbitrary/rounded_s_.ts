/**
 * - border-start-start-radius: ${arbitrary};
 * - border-end-start-radius: <value>;
 */
export const rounded_s_ = (arbitrary: string) => () => `  border-start-start-radius: ${arbitrary};
  border-end-start-radius: <value>;`;
