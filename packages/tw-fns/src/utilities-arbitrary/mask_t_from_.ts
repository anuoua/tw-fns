/**
 * - mask-image: linear-gradient(to top, black ${arbitrary}, transparent var(--tw-mask-top-to));
 */
export const mask_t_from_ = (arbitrary: string) => () => `  mask-image: linear-gradient(to top, black ${arbitrary}, transparent var(--tw-mask-top-to));`;
