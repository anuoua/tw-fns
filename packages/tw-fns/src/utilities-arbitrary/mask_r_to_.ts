/**
 * - mask-image: linear-gradient(to right, black var(--tw-mask-right-from), transparent ${arbitrary});
 */
export const mask_r_to_ = (arbitrary: string) => () => `  mask-image: linear-gradient(to right, black var(--tw-mask-right-from), transparent ${arbitrary});`;
