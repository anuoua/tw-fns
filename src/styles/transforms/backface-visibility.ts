import { createStyle } from "../../core/style";

// Backface visibility utilities
export const backface_visible = createStyle("backface-visibility", "visible");
export const backface_hidden = createStyle("backface-visibility", "hidden");