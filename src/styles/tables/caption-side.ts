import { createStyle } from "../../core/style";

// Caption side utilities
export const caption_top = createStyle("caption-side", "top");
export const caption_bottom = createStyle("caption-side", "bottom");