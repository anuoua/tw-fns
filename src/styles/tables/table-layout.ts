import { createStyle } from "../../core/style";

// Table layout utilities
export const table_auto = createStyle("table-layout", "auto");
export const table_fixed = createStyle("table-layout", "fixed");