import { createStyle } from "../../core/style";

// Border collapse utilities
export const border_collapse = createStyle("border-collapse", "collapse");
export const border_separate = createStyle("border-collapse", "separate");