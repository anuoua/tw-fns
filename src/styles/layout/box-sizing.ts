import { createStyle } from "../../core/style";

// box-sizing utilities
export const box_border = createStyle("box-sizing", "border-box");
export const box_content = createStyle("box-sizing", "content-box");
