import { createStyle } from "../../core/style";

// break-inside utilities
export const break_inside_auto = createStyle("break-inside", "auto");
export const break_inside_avoid = createStyle("break-inside", "avoid");
export const break_inside_avoid_page = createStyle("break-inside", "avoid-page");
export const break_inside_avoid_column = createStyle("break-inside", "avoid-column");
