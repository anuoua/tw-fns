import { createStyle } from "../../core/style";

export const flex_nowrap = createStyle("flex-wrap", "nowrap");
export const flex_wrap = createStyle("flex-wrap", "wrap");
export const flex_wrap_reverse = createStyle("flex-wrap", "wrap-reverse");