import { createStyle } from "../../core/style";

// Forced color adjust
export const forced_color_adjust_auto = createStyle("forced-color-adjust", "auto");
export const forced_color_adjust_none = createStyle("forced-color-adjust", "none");