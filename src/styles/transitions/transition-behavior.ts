import { createStyle } from "../../core/style";

// Transition behavior utilities
export const transition_normal = createStyle("transition-behavior", "normal");
export const transition_allow_discrete = createStyle("transition-behavior", "allow-discrete");