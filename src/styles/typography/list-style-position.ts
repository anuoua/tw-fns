import { createStyle } from "../../core/style";

// List style position utilities
export const list_inside = createStyle("list-style-position", "inside");
export const list_outside = createStyle("list-style-position", "outside");