import { createStyle } from "../../core/style";

// Text wrap utilities
export const text_wrap = createStyle("text-wrap", "wrap");
export const text_nowrap = createStyle("text-wrap", "nowrap");
export const text_balance = createStyle("text-wrap", "balance");
export const text_pretty = createStyle("text-wrap", "pretty");