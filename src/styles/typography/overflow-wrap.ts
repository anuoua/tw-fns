import { createStyle } from "../../core/style";

// Overflow wrap utilities
export const overflow_wrap_normal = createStyle("overflow-wrap", "normal");
export const overflow_wrap_anywhere = createStyle("overflow-wrap", "anywhere");
export const overflow_wrap_break_word = createStyle("overflow-wrap", "break-word");