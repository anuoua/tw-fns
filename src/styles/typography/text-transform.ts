import { createStyle } from "../../core/style";

// Text transform utilities
export const uppercase = createStyle("text-transform", "uppercase");
export const lowercase = createStyle("text-transform", "lowercase");
export const capitalize = createStyle("text-transform", "capitalize");
export const normal_case = createStyle("text-transform", "none");