import { createStyle } from "../../core/style";

// Mask type utilities
export const mask_luminance_type = createStyle("-webkit-mask-type", "luminance");
export const mask_alpha_type = createStyle("-webkit-mask-type", "alpha");