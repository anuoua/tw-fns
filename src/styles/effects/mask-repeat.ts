import { createStyle } from "../../core/style";

// Mask repeat utilities
export const mask_repeat = createStyle("-webkit-mask-repeat", "repeat");
export const mask_no_repeat = createStyle("-webkit-mask-repeat", "no-repeat");
export const mask_repeat_x = createStyle("-webkit-mask-repeat", "repeat-x");
export const mask_repeat_y = createStyle("-webkit-mask-repeat", "repeat-y");
export const mask_repeat_round = createStyle("-webkit-mask-repeat", "round");
export const mask_repeat_space = createStyle("-webkit-mask-repeat", "space");