import { createStyle } from "../../core/style";

// Mask mode utilities
export const mask_alpha = createStyle("-webkit-mask-mode", "alpha");
export const mask_luminance = createStyle("-webkit-mask-mode", "luminance");
export const mask_match_source = createStyle("-webkit-mask-mode", "match-source");