import { createStyle } from "../../core/style";

// Background attachment utilities
export const bg_fixed = createStyle("background-attachment", "fixed");
export const bg_local = createStyle("background-attachment", "local");
export const bg_scroll = createStyle("background-attachment", "scroll");