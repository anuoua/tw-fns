import { createStyle } from "../../core/style";

// Background origin utilities
export const bg_origin_border = createStyle("background-origin", "border-box");
export const bg_origin_padding = createStyle("background-origin", "padding-box");
export const bg_origin_content = createStyle("background-origin", "content-box");