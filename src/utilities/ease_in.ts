/**
 * --tw-ease: var(--ease-in);
 *
 * transition-timing-function: var(--ease-in);
 */
export const ease_in = () => `
    --tw-ease: var(--ease-in);
    transition-timing-function: var(--ease-in);
  `;
